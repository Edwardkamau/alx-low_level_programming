#include "main.h"

/**
 * mul - prints the multiple of two integers
 *
 * @a: number one
 * @b: number two
 *
 * Return: multiple of number 1 and number 2
 */

int mul(int a, int b)
{
	return (a * b);
}
